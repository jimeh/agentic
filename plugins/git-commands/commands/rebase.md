---
description: Rebase the current branch and review upstream integration
---

Use the installed `rebase` skill to handle this request. Follow that skill as
the source of truth for checkout safety, upstream selection, conflict handling,
validation, and any separately authorized publication.

If the skill is unavailable, stop and explain that this deprecated command does
not carry an independent fallback workflow.
