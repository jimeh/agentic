interface:
  display_name: "Claude Review"
  short_description: "Independent Claude review of code changes"
  default_prompt:
    "Use $claude-review to get an independent Claude review of the current
    changes, including correctness, proportionate validation evidence, test
    quality, and residual risk; verify its findings against the code and report
    them."
