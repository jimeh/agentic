interface:
  display_name: "Git Rebase"
  short_description: "Rebase safely and resolve conflicts by intent"
  default_prompt:
    "Use $rebase to rebase safely onto the requested base and resolve conflicts
    by intent."
