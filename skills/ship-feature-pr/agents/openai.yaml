interface:
  display_name: "Ship Feature PR"
  short_description: "Implement and deliver a reviewed feature PR"
  default_prompt:
    "Use $ship-feature-pr to implement this feature and deliver it as a
    reviewed, ready pull request."
