interface:
  display_name: "Vuln Scan Orchestrator"
  short_description: "Plan and orchestrate repo security scans"
  default_prompt: "Use $vuln-scan-orchestrator to analyze this project, create a security playbook, split it into focus areas, and coordinate sub-agents to review each area."
